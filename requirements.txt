# Core — install RAPIDS (cudf) via conda, NOT pip. See environment.yml.
# pip requirements (inside the RAPIDS conda env):
ultralytics>=8.2.0
torch>=2.2.0
torchvision>=0.17.0
opencv-python>=4.9.0
numpy>=1.26.0
av>=12.0.0            # PyAV decode
numba>=0.59.0
cupy-cuda12x>=13.0.0
pyarrow>=15.0.0
rich>=13.7.0          # console table
onnxruntime>=1.17.0   # CPU baseline for benchmark
pandas>=2.2.0         # CPU baseline for benchmark
pyyaml>=6.0.1
